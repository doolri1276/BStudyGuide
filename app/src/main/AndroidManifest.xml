<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.snownaul.bstudyguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_icon01"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_icon01_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Main" />
        <activity android:name=".AddSetActivity" />
        <activity
            android:name=".SetDetailActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".SetDetailStorageActivity" />
        <activity android:name=".SetDetailStudyActivity" />
        <activity android:name=".SetDetailPlayActivity" />
        <activity android:name=".SetDetailReportActivity"></activity>
    </application>

</manifest>